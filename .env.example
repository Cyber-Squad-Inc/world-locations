# Required for GeoNames city enrichment (free signup: https://www.geonames.org/login)
GEONAMES_USER=your_geonames_username

# Optional: Use a self-hosted Overpass instance to avoid public rate limits
OVERPASS_URL=https://overpass-api.de/api/interpreter

# Throttling (ms) – be nice to public APIs
RESTCOUNTRIES_DELAY=100
COUNTRIESNOW_DELAY=150
GEONAMES_DELAY=2000
OVERPASS_DELAY=3000
